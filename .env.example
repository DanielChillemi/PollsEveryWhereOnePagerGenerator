# Marketing One-Pager Co-Creation Tool - Environment Configuration
# ==================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# ============================================================
# Server Configuration
# ============================================================
API_HOST=0.0.0.0
API_PORT=8000
API_ENV=development  # development, staging, production

# ============================================================
# Database Configuration
# ============================================================
# Local MongoDB
MONGODB_URL=mongodb://localhost:27017

# MongoDB Atlas (cloud) - Uncomment and use for production
# MONGODB_URL=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority

MONGODB_DB_NAME=marketing_onepager

# ============================================================
# JWT Authentication
# ============================================================
# Generate strong secrets using: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
JWT_REFRESH_SECRET_KEY=your-super-secret-refresh-key-change-this-too
JWT_ALGORITHM=HS256

# Token expiration times (in minutes for access, days for refresh)
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ============================================================
# CORS (Cross-Origin Resource Sharing)
# ============================================================
# Frontend URL(s) - comma-separated for multiple origins
FRONTEND_URL=http://localhost:5173
# For production, add: https://your-app.vercel.app

# ============================================================
# Canva Connect API
# ============================================================
CANVA_API_BASE_URL=https://api.canva.com/rest
CANVA_CLIENT_ID=your-canva-client-id
CANVA_CLIENT_SECRET=your-canva-client-secret
CANVA_REDIRECT_URI=http://localhost:8000/api/v1/canva/callback

# OAuth token (temporary - will be replaced with OAuth flow)
CANVA_ACCESS_TOKEN=your-canva-oauth-token

# ============================================================
# AI Integration (OpenAI)
# ============================================================
# Get your API key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# AI Model Selection
# Options: gpt-4-turbo-preview (best quality), gpt-3.5-turbo (faster/cheaper), gpt-4 (legacy)
AI_MODEL_NAME=gpt-4-turbo-preview

# Alternative AI providers (optional, not currently used)
# HUGGINGFACE_API_TOKEN=your-huggingface-token
# GEMINI_API_KEY=your-google-gemini-api-key

# ============================================================
# Logging & Monitoring
# ============================================================
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL
